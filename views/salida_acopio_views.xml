<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <record id="action_salida_acopio" model="ir.actions.act_window">
        <field name="name">Salidas de Acopio</field>
        <field name="res_model">salida.acopio</field>
        <field name="view_mode">list,form</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                No hay salidas de acopio registradas
            </p>
        </field>
    </record>

    <record id="action_salida_acopio_wizard" model="ir.actions.act_window">
        <field name="name">Nueva Salida de Acopio</field>
        <field name="res_model">salida.acopio.wizard</field>
        <field name="view_mode">form</field>
        <field name="target">new</field>
    </record>

    <record id="view_salida_acopio_list" model="ir.ui.view">
        <field name="name">salida.acopio.list</field>
        <field name="model">salida.acopio</field>
        <field name="arch" type="xml">
            <list string="Salidas de Acopio" default_order="fecha_salida desc">
                <field name="numero_referencia"/>
                <field name="transportista_id"/>
                <field name="destinatario_id"/>
                <field name="chofer_id" optional="show"/>
                <field name="vehicle_id" optional="hide"/>
                <field name="numero_placa" optional="show"/>
                <field name="fecha_salida"/>
                <field name="usuario_salida"/>
                <field name="total_residuos"/>
                <field name="cantidad_total"/>
                <field name="manifiesto_salida_id"/>
                <field name="state"
                       widget="badge"
                       decoration-success="state == 'done'"
                       decoration-muted="state == 'cancel'"/>
                <button name="action_view_picking"
                        type="object"
                        string="Ver Transferencia"
                        class="btn-link"
                        invisible="state != 'done'"/>
                <button name="action_view_manifiesto"
                        type="object"
                        string="Ver Manifiesto"
                        class="btn-link"
                        invisible="state != 'done'"/>
            </list>
        </field>
    </record>

    <record id="view_salida_acopio_form" model="ir.ui.view">
        <field name="name">salida.acopio.form</field>
        <field name="model">salida.acopio</field>
        <field name="arch" type="xml">
            <form string="Salida de Acopio">
                <header>
                    <button name="action_confirmar_salida"
                            string="Confirmar Salida"
                            type="object"
                            class="btn-primary"
                            invisible="state != 'draft'"
                            confirm="¿Está seguro de confirmar esta salida? Se crearán movimientos de inventario y el manifiesto."/>
                    <button name="action_view_picking"
                            string="Ver Transferencia"
                            type="object"
                            class="btn-secondary"
                            invisible="state != 'done'"/>
                    <button name="action_view_manifiesto"
                            string="Ver Manifiesto"
                            type="object"
                            class="btn-secondary"
                            invisible="state != 'done'"/>
                    <button name="action_cancelar"
                            string="Cancelar"
                            type="object"
                            invisible="state != 'draft'"
                            confirm="¿Está seguro de cancelar esta salida?"/>
                    <field name="state" widget="statusbar" statusbar_visible="draft,done"/>
                </header>

                <sheet>
                    <div class="oe_button_box" name="button_box">
                        <button name="action_view_picking"
                                type="object"
                                class="oe_stat_button"
                                icon="fa-truck"
                                invisible="state != 'done'">
                            <div class="o_field_widget o_stat_info">
                                <span class="o_stat_text">Transferencia</span>
                            </div>
                        </button>
                        <button name="action_view_manifiesto"
                                type="object"
                                class="oe_stat_button"
                                icon="fa-file-text-o"
                                invisible="state != 'done'">
                            <div class="o_field_widget o_stat_info">
                                <span class="o_stat_text">Manifiesto Generado</span>
                            </div>
                        </button>
                    </div>

                    <div class="oe_title">
                        <h1><field name="numero_referencia" readonly="1"/></h1>
                    </div>

                    <div class="alert alert-success"
                         style="margin-bottom: 20px;"
                         invisible="state != 'done'">
                        <strong>✅ Manifiesto Generado:</strong>
                        <field name="manifiesto_salida_id" readonly="1"/>
                        (SAI como generador)
                    </div>

                    <!-- Bloque 1: Transporte + Vehículo/Operador (lado a lado) -->
                    <group>
                        <group string="Información del Transporte">
                            <field name="transportista_id"
                                   readonly="state != 'draft'"
                                   options="{'no_create': True}"/>
                            <field name="destinatario_id"
                                   readonly="state != 'draft'"
                                   options="{'no_create': True}"/>
                            <field name="fecha_salida" readonly="state != 'draft'"/>
                            <field name="usuario_salida" readonly="1"/>
                        </group>
                        <group string="Datos del Vehículo y Operador">
                            <field name="chofer_id"
                                   readonly="state != 'draft'"
                                   options="{'no_create': True}"
                                   placeholder="Seleccionar chofer..."/>
                            <field name="vehicle_id"
                                   readonly="state != 'draft'"
                                   options="{'no_create': True}"
                                   placeholder="Seleccionar vehículo de flota..."/>
                            <field name="numero_placa"
                                   readonly="1"
                                   placeholder="Se rellena desde el vehículo"/>
                        </group>
                    </group>

                    <!-- Bloque 2: Resumen + Compañía (lado a lado) -->
                    <group>
                        <group string="Resumen">
                            <field name="total_residuos" readonly="1"/>
                            <field name="cantidad_total" readonly="1"/>
                        </group>
                        <group string="Compañía" groups="base.group_multi_company">
                            <field name="company_id" readonly="state != 'draft'"/>
                        </group>
                    </group>

                    <notebook>
                        <page string="Líneas de Salida">
                            <field name="linea_ids" readonly="state != 'draft'">
                                <list editable="bottom">
                                    <field name="available_product_ids" column_invisible="1"/>
                                    <field name="producto_id"
                                           domain="[('id', 'in', available_product_ids)]"
                                           options="{'no_create': True}"/>
                                    <field name="available_lot_ids" column_invisible="1"/>
                                    <field name="lote_id"
                                           domain="[('id', 'in', available_lot_ids)]"
                                           options="{'no_create': True, 'no_open': True}"/>
                                    <field name="nombre_residuo"/>
                                    <field name="stock_disponible" readonly="1"/>
                                    <field name="cantidad"/>
                                    <field name="clasificacion_corrosivo" string="C" optional="show"/>
                                    <field name="clasificacion_reactivo" string="R" optional="show"/>
                                    <field name="clasificacion_explosivo" string="E" optional="show"/>
                                    <field name="clasificacion_toxico" string="T" optional="show"/>
                                    <field name="clasificacion_inflamable" string="I" optional="show"/>
                                    <field name="clasificacion_biologico" string="B" optional="show"/>
                                    <field name="clasificaciones_cretib" readonly="1" optional="hide"/>
                                    <field name="tipo_manejo_id" optional="show"/>
                                    <field name="envase_cantidad" optional="hide"/>
                                    <field name="packaging_id" optional="hide"/>
                                    <field name="envase_capacidad" optional="hide"/>
                                    <field name="residue_type" optional="hide"/>
                                </list>
                                <form string="Detalle de Línea de Salida">
                                    <sheet>
                                        <group>
                                            <group string="Producto y Lote">
                                                <field name="available_product_ids" invisible="1"/>
                                                <field name="producto_id"
                                                       domain="[('id', 'in', available_product_ids)]"
                                                       options="{'no_create': True}"/>
                                                <field name="available_lot_ids" invisible="1"/>
                                                <field name="lote_id"
                                                       domain="[('id', 'in', available_lot_ids)]"
                                                       options="{'no_create': True, 'no_open': True}"/>
                                                <field name="stock_disponible" readonly="1"/>
                                                <field name="cantidad"/>
                                            </group>
                                            <group string="Identificación del Residuo">
                                                <field name="nombre_residuo"/>
                                                <field name="residue_type"/>
                                                <field name="tipo_manejo_id" options="{'no_create': True}"/>
                                            </group>
                                        </group>

                                        <group string="Clasificación CRETIB" col="6">
                                            <field name="clasificacion_corrosivo"/>
                                            <field name="clasificacion_reactivo"/>
                                            <field name="clasificacion_explosivo"/>
                                            <field name="clasificacion_toxico"/>
                                            <field name="clasificacion_inflamable"/>
                                            <field name="clasificacion_biologico"/>
                                        </group>

                                        <group>
                                            <group string="Envase">
                                                <field name="envase_cantidad" string="Unidades"/>
                                                <field name="packaging_id" string="Embalaje"/>
                                                <field name="envase_tipo" string="Tipo (Legacy)"/>
                                                <field name="envase_capacidad"/>
                                            </group>
                                            <group string="Etiquetado">
                                                <field name="etiqueta_si"/>
                                                <field name="etiqueta_no"/>
                                            </group>
                                        </group>
                                    </sheet>
                                </form>
                            </field>
                        </page>

                        <page string="Observaciones">
                            <group>
                                <field name="observaciones" nolabel="1"
                                       placeholder="Observaciones adicionales..."
                                       readonly="state != 'draft'"/>
                            </group>
                        </page>
                    </notebook>
                </sheet>
            </form>
        </field>
    </record>

    <record id="view_salida_acopio_search" model="ir.ui.view">
        <field name="name">salida.acopio.search</field>
        <field name="model">salida.acopio</field>
        <field name="arch" type="xml">
            <search string="Buscar Salidas de Acopio">
                <field name="numero_referencia"/>
                <field name="transportista_id"/>
                <field name="destinatario_id"/>
                <field name="manifiesto_salida_id"/>
                <field name="usuario_salida"/>
                <field name="chofer_id"/>
                <field name="vehicle_id"/>
                <field name="numero_placa"/>
                <filter string="Borradores" name="draft" domain="[('state','=','draft')]"/>
                <filter string="Realizadas" name="done" domain="[('state','=','done')]"/>
                <filter string="Canceladas" name="cancel" domain="[('state','=','cancel')]"/>
                <filter string="Hoy" name="today"
                        domain="[('fecha_salida', '&gt;=', context_today().strftime('%Y-%m-%d'))]"/>
                <filter string="Esta Semana" name="this_week"
                        domain="[('fecha_salida', '&gt;=', (context_today() - datetime.timedelta(days=7)).strftime('%Y-%m-%d'))]"/>
                <filter string="Este Mes" name="this_month"
                        domain="[('fecha_salida', '&gt;=', context_today().strftime('%Y-%m-01'))]"/>
                <filter string="Estado" name="group_state" context="{'group_by':'state'}"/>
                <filter string="Transportista" name="group_transportista" context="{'group_by':'transportista_id'}"/>
                <filter string="Destinatario" name="group_destinatario" context="{'group_by':'destinatario_id'}"/>
                <filter string="Chofer" name="group_chofer" context="{'group_by':'chofer_id'}"/>
                <filter string="Vehículo" name="group_vehicle" context="{'group_by':'vehicle_id'}"/>
                <filter string="Placa" name="group_placa" context="{'group_by':'numero_placa'}"/>
                <filter string="Usuario" name="group_user" context="{'group_by':'usuario_salida'}"/>
                <filter string="Fecha" name="group_date" context="{'group_by':'fecha_salida:day'}"/>
            </search>
        </field>
    </record>
</odoo>